# Скопируй в .env.local и заполни.

# Яндекс.Метрика — номер счётчика (например, 98765432).
# Без него счётчик не подключается. NEXT_PUBLIC_ — значение попадает в клиент.
NEXT_PUBLIC_YANDEX_METRICA_ID=
